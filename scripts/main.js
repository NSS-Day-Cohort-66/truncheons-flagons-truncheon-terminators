import {
  renderInitialHTML,
  renderGameHTML,
  renderCreationHTML,
  renderLeaderboardHTML,
  displayFinalScores,
  displayWinner
} from "./GameState.js";

renderInitialHTML();

renderGameHTML(final)